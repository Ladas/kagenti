apiVersion: agent.kagenti.dev/v1alpha1
kind: Agent
metadata:
  labels:
    app.kubernetes.io/created-by: e2e-test
    app.kubernetes.io/name: kagenti-operator
    kagenti.io/framework: AutoGen
    kagenti.io/protocol: a2a
    kagenti.io/type: agent
  name: orchestrator-agent
  namespace: kagenti-agents
  annotations:
    description: "Orchestrator agent that routes tasks to specialized agents via A2A"
spec:
  description: "Agent that discovers and delegates tasks to appropriate specialized agents using A2A protocol"

  replicas: 1

  imageSource:
    buildRef:
      name: orchestrator-agent-build

  podTemplateSpec:
    spec:
      containers:
      - name: agent
        env:
        # Service Configuration
        - name: PORT
          value: "8000"
        - name: HOST
          value: "0.0.0.0"

        # MCP Tool Configuration (a2a-bridge for agent discovery/communication)
        - name: MCP_URL
          value: "http://mcp-a2a-bridge-proxy.kagenti-agents.svc.cluster.local:8080/mcp"
        - name: MCP_TRANSPORT
          value: "http"

        # OpenTelemetry Configuration
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.kagenti-system.svc.cluster.local:8335"
        - name: OTEL_SERVICE_NAME
          value: "orchestrator-agent"
        - name: OTEL_EXPORTER_OTLP_PROTOCOL
          value: "http/protobuf"

        # Phoenix Project Assignment
        - name: PHOENIX_PROJECT_NAME
          value: "default"

        # Kubernetes Metadata (via Downward API)
        - name: K8S_NAMESPACE_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name

        # Deployment Environment
        - name: DEPLOYMENT_ENVIRONMENT
          value: "kind-local"

        # LLM Configuration (OpenAI API)
        - name: LLM_API_BASE
          value: "https://api.openai.com/v1"
        - name: LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: openai-api-key
              key: api-key
        - name: TASK_MODEL_ID
          value: "gpt-4o-mini"

        ports:
        - containerPort: 8000
          name: http
          protocol: TCP

        resources:
          limits:
            cpu: 500m
            memory: 1Gi
          requests:
            cpu: 100m
            memory: 256Mi

        volumeMounts:
        - mountPath: /app/.cache
          name: cache

      volumes:
      - name: cache
        emptyDir: {}
